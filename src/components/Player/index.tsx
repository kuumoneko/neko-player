export default function Player({ seturl }: { seturl: (url: string) => void }) {
    return (
        <div className="w-[80%] h-[5%]">
            <div className="bg-slate-600 h-full w-full rounded-4xl"></div>
        </div>
    );
}
